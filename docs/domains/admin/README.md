## Domain
Admin capabilities in CollabSphere, including admin dashboards and privileged management of users, workspaces, and audit access.

## Canonical Sources
- `docs/spec/05-features/` (admin features and dashboards)
- `docs/spec/02-personas-roles/` (admin roles and permissions)
- `docs/spec/11-security/` (admin access, least privilege, and safety controls)

## Included Topics
- Admin dashboard surfaces
- User and workspace management by admins
- Access to audit logs and sensitive data
- Admin-only APIs and safety checks

## Related domains
- `activity-audit/` — audit log access
- `auth/` — user and session implications of admin actions
- `workspaces/` — workspace lifecycle management
