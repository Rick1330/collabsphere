# Legacy Alias: Next.js Page Implementation

This skill name is kept only for backward compatibility.

The current CollabSphere frontend runtime is Vite + React SPA, not Next.js.

Use:
- `implementation/web-page.md`

Label guidance:
- prefer `spec:web-page`
- treat `spec:nextjs-page` as deprecated alias
