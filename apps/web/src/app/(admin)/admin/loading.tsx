import { SegmentLoading } from "../../../components/foundation/segment-loading";

export default function AdminLoading() {
  return (
    <SegmentLoading
      label="Loading admin route"
      detail="Preparing the admin route foundation."
    />
  );
}
